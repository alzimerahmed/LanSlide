.idea

# Pub workspace resolution output
/.dart_tool/

# Rust workspace build output
/target/

.refena_inspector/

# Local signing secrets (keystore + key.properties live here for local release builds)
/secrets

# FVM Version Cache
.fvm/

# Devin prompt system + per-project knowledge layer (private, never pushed)
.devin/
docs/*.md
LandSlide icon.png
